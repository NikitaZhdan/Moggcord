// Публичный STUN Google — этого достаточно для локальной разработки и части
// реальных сетей (когда NAT "простой"). Но для прода почти наверняка нужен
// ещё и TURN-сервер (например self-hosted coturn): без него звонок не
// пройдёт через симметричный NAT / часть корпоративных и мобильных сетей —
// P2P-соединение просто не установится, и ни один STUN тут не поможет,
// потому что STUN лишь помогает узнать свой публичный адрес, а не
// ретранслирует трафик. Добавьте сюда urls/username/credential вашего TURN,
// когда он будет поднят:
//
// { urls: 'turn:turn.example.com:3478', username: '...', credential: '...' }
export const ICE_SERVERS = [{ urls: 'stun:stun.l.google.com:19302' }]
